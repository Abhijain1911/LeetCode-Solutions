class Solution {
    public int lengthOfLastWord(String s) {
        s=s.strip();
        int len=s.length()-1;
        int c=0;
        while(len>=0 && s.charAt(len)!=' '){
            c++;
            len--;
        }
        return c;
    }
}
